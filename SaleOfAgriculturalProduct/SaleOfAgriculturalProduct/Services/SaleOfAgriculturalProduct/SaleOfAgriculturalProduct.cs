using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace SaleOfAgriculturalProduct.Services
{
    public interface SaleOfAgriculturalProduct<T>
    {
        //Task<Item<T>> AddAsync(Item<T> item);

        //Task DeleteAsync(Item<T, TColor> item);

        //Task EditAsync(Item<T, TColor> item);

        //Task<Item<T, TColor>> GetItem(T id);

        //Task<IEnumerable<Item<T, TColor>>> GetItemsAsync(
        //    OrderBy orderBy = OrderBy.None,
        //    int offset = 0,
        //    int limit = 10,
        //    bool? completed = null,
        //    TColor colorFilter = null,
        //    string titleFilter = null
        //);
    }
}
